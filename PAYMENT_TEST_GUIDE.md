# 결제 기능 테스트 가이드

## 📋 테스트 전 준비사항

### 1. 환경 변수 확인 (선택사항)

프로젝트 루트에 `.env.local` 파일이 있다면 다음을 추가하세요:
```env
NEXT_PUBLIC_TOSS_PAYMENTS_WIDGET_CLIENT_KEY=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
TOSS_PAYMENTS_WIDGET_SECRET_KEY=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
```

**참고**: 환경 변수를 설정하지 않아도 코드에 기본값이 있어 테스트 가능합니다.

### 2. 개발 서버 실행

```bash
npm run dev
```

서버가 실행되면 `http://localhost:3000`에서 접속 가능합니다.

---

## 🧪 테스트 시나리오

### 시나리오 1: 기본 결제 플로우 테스트

1. **게임 접속**
   - `http://localhost:3000/watermelon` 접속
   - 닉네임과 패스워드 입력 후 로그인

2. **아이템 상점 열기**
   - 게임 화면에서 **🛒 아이템** 버튼 클릭
   - 아이템 상점 모달이 열리는지 확인

3. **아이템 선택 및 결제 시작**
   - 원하는 아이템의 **구매하기** 버튼 클릭
   - 결제위젯이 표시되는지 확인
   - 결제 수단 선택 UI가 보이는지 확인

4. **결제 정보 입력 (테스트)**
   - **신용·체크 카드** 선택
   - 테스트 카드 정보 입력:
     - 카드번호: `1234-5678-9012-3456`
     - 유효기간: 임의의 미래 날짜 (예: `12/25`)
     - CVC: 임의의 3자리 숫자 (예: `123`)
     - 카드 비밀번호: 임의의 2자리 숫자 (예: `12`)
   - **결제하기** 버튼 클릭

5. **결제 완료 확인**
   - 결제 성공 페이지로 리다이렉트되는지 확인
   - "결제 완료!" 메시지 확인
   - 3초 후 게임 페이지로 자동 이동 확인

6. **아이템 지급 확인**
   - 게임 페이지로 돌아온 후 토스트 메시지 확인
   - (향후 구현) 인벤토리에서 아이템 확인

### 시나리오 2: 결제 실패 테스트

1. **결제 취소**
   - 결제위젯에서 **취소** 버튼 클릭
   - 모달이 닫히는지 확인

2. **결제 실패 시뮬레이션**
   - 결제 정보 입력 중 브라우저 뒤로가기
   - 또는 결제창에서 취소
   - 결제 실패 페이지로 이동하는지 확인

---

## 🔍 각 단계별 확인 사항

### ✅ 결제위젯 렌더링 확인
- [ ] 결제 수단 선택 UI가 표시됨
- [ ] 이용약관 UI가 표시됨
- [ ] 결제 금액이 올바르게 표시됨
- [ ] "결제하기" 버튼이 활성화됨

### ✅ 결제 요청 확인
- [ ] 결제 버튼 클릭 시 로딩 상태 표시
- [ ] 결제창이 정상적으로 열림
- [ ] 브라우저 콘솔에 에러가 없음

### ✅ 결제 승인 확인
- [ ] 결제 성공 페이지로 이동
- [ ] 서버 로그에 결제 승인 성공 메시지
- [ ] 데이터베이스에 결제 내역 저장 확인
- [ ] 플레이어 인벤토리에 아이템 추가 확인

---

## 🐛 문제 해결

### 문제 1: 결제위젯이 표시되지 않음

**증상**: 아이템 선택 후 결제위젯이 보이지 않음

**해결 방법**:
1. 브라우저 콘솔(F12)에서 에러 확인
2. 네트워크 탭에서 토스페이먼츠 SDK 로드 확인
3. 환경 변수 `NEXT_PUBLIC_TOSS_PAYMENTS_WIDGET_CLIENT_KEY` 확인

**확인 코드**:
```javascript
// 브라우저 콘솔에서 실행
console.log(process.env.NEXT_PUBLIC_TOSS_PAYMENTS_WIDGET_CLIENT_KEY);
```

### 문제 2: 결제 승인 실패

**증상**: 결제는 진행되었지만 승인 실패

**해결 방법**:
1. 서버 로그 확인 (터미널)
2. 결제 금액이 일치하는지 확인
3. `TOSS_PAYMENTS_WIDGET_SECRET_KEY` 확인

**확인 사항**:
- 서버 로그에서 에러 메시지 확인
- `/api/watermelon/payments/approve` API 응답 확인

### 문제 3: 결제 후 아이템이 지급되지 않음

**증상**: 결제는 성공했지만 인벤토리에 아이템이 없음

**해결 방법**:
1. 데이터베이스 확인:
   ```sql
   -- 결제 내역 확인
   SELECT * FROM watermelon_payments WHERE status = 'completed' ORDER BY created_at DESC LIMIT 5;
   
   -- 아이템 구매 내역 확인
   SELECT * FROM watermelon_item_purchases ORDER BY created_at DESC LIMIT 5;
   
   -- 플레이어 인벤토리 확인
   SELECT * FROM watermelon_player_items WHERE player_id = 'YOUR_PLAYER_ID';
   ```

2. 서버 로그에서 아이템 지급 로직 실행 여부 확인

---

## 📊 테스트 체크리스트

### 기본 기능
- [ ] 아이템 상점 모달 열기/닫기
- [ ] 아이템 목록 표시
- [ ] 결제위젯 렌더링
- [ ] 결제 수단 선택
- [ ] 결제 정보 입력
- [ ] 결제 요청
- [ ] 결제 승인
- [ ] 결제 성공 페이지
- [ ] 결제 실패 페이지

### 에러 처리
- [ ] 로그인 없이 구매 시도 → 에러 메시지
- [ ] 결제 취소 → 모달 닫힘
- [ ] 결제 실패 → 실패 페이지 표시
- [ ] 네트워크 오류 → 적절한 에러 처리

### 데이터 확인
- [ ] 결제 내역 DB 저장
- [ ] 아이템 구매 내역 DB 저장
- [ ] 플레이어 인벤토리 업데이트

---

## 🎯 빠른 테스트 명령어

### 데이터베이스 확인 (Prisma Studio)
```bash
npx prisma studio
```
브라우저에서 `http://localhost:5555` 접속하여 데이터 확인

### 서버 로그 확인
개발 서버 터미널에서 다음 로그 확인:
- `Payment request API error:` - 결제 요청 에러
- `Payment approval API error:` - 결제 승인 에러
- `[GAME OVER]` - 게임 오버 로그

---

## 💡 테스트 팁

1. **브라우저 개발자 도구 활용**
   - F12로 콘솔 열기
   - Network 탭에서 API 호출 확인
   - Application 탭에서 Local Storage 확인

2. **여러 결제 수단 테스트**
   - 신용·체크 카드
   - 계좌이체 (테스트 모드)
   - 간편결제 (테스트 모드)

3. **에러 시나리오 테스트**
   - 결제 중 브라우저 닫기
   - 네트워크 끊기
   - 잘못된 카드 정보 입력

---

## 🚀 다음 단계

테스트가 성공적으로 완료되면:
1. 아이템 사용 기능 구현 (7단계)
2. 게임 로직에 아이템 효과 통합 (8단계)

문제가 발생하면 에러 메시지와 함께 알려주세요!
